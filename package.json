{
  "name": "relator-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension para transcrever reuniões com Whisper local e resumir com Gemini.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
